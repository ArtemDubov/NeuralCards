:root {
  --color-sea-wave: #4fc3f7;
  --color-deep-ocean: #0a1a2d;
  --color-light-foam: #e3f2fd;
  --color-neural-accent: #7e57c2;
}

.auth-tabs {
  display: flex;
  background: rgba(30, 58, 95, 0.8);
  border-radius: 12px;
  padding: 0.3rem;
  margin-bottom: 1.5rem;
  gap: 0.3rem;
}

.tab-button {
  flex: 1;
  padding: 0.8rem 1.5rem;
  border: none;
  background: transparent;
  color: var(--color-light-foam);
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
  font-size: 1rem;
}

.tab-button:hover {
  background: rgba(79, 195, 247, 0.2);
}

.tab-button.active {
  background: var(--color-sea-wave);
  color: var(--color-deep-ocean);
  font-weight: bold;
}

