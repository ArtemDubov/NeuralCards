Remove-Item "text" -Recurse -Force -ErrorAction SilentlyContinue; mkdir text; Get-ChildItem -Recurse -File | Where-Object { ($_.Extension -in '.js','.css','.html','.json','.md','.txt','.prisma') -and $_.DirectoryName -notmatch "\\text" -and $_.DirectoryName -notmatch "\\node_modules" -and $_.Name -notmatch "^(package\.json|package-lock\.json)$" } | ForEach-Object { $n=$_.FullName.Replace("$PWD\","").Replace("\","_"); $content = Get-Content $_.FullName -Raw -Encoding UTF8; if (!$content) { $content = "// Empty file" }; $content | Out-File "text\$n.txt" -Encoding UTF8; Write-Host "✓ $n" }; "Структура проекта:`n" + (Get-ChildItem -Recurse | Where-Object { $_.FullName -notmatch '\\text|\\node_modules' } | ForEach-Object { $_.FullName.Replace("$PWD\","") }) | Out-File "text\structure.txt" -Encoding UTF8
