/* Глобальные CSS переменные */
:root {
  --color-deep-ocean: #0a1a2d;
  --color-ocean-blue: #1e3a5f;
  --color-sea-wave: #4fc3f7;
  --color-neural-accent: #7e57c2;
  --color-light-foam: #e3f2fd;
  --color-text-light: #ffffff;
  --color-success: #4caf50;
}

/* Базовые стили для стандартных полей ввода */
input:not(.search-input):not(.no-global-styles),
textarea:not(.search-input):not(.no-global-styles),
select:not(.search-input):not(.no-global-styles) {
  width: 100%;
  padding: 0.8rem 1rem;
  border: 1px solid var(--color-sea-wave);
  border-radius: 8px;
  background: rgba(227, 242, 253, 0.1);
  color: var(--color-text-light);
  font-size: 1rem;
  margin-bottom: 1rem;
}

input:not(.search-input):not(.no-global-styles):focus,
textarea:not(.search-input):not(.no-global-styles):focus,
select:not(.search-input):not(.no-global-styles):focus {
  outline: none;
  border-color: var(--color-neural-accent);
  box-shadow: 0 0 10px rgba(126, 87, 194, 0.3);
}

input:not(.search-input):not(.no-global-styles)::placeholder,
textarea:not(.search-input):not(.no-global-styles)::placeholder {
  color: rgba(255, 255, 255, 0.6);
}

/* Глобальные стили для кнопок */
button {
  background: var(--color-sea-wave);
  color: var(--color-deep-ocean);
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
  cursor: pointer;
  font-size: 16px;
  font-weight: 600;
  transition: all 0.3s ease;
  margin: 0.25rem;
}

/* ИЗМЕНИЛ: убрал глобальный hover для всех кнопок */
/* button:hover {
  background: var(--color-neural-accent);
  color: var(--color-text-light);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(126, 87, 194, 0.3);
} */

button:active {
  transform: translateY(0);
}

/* Специфичные стили для кнопок с классами */
button.action-button {
  background: linear-gradient(
    135deg,
    var(--color-sea-wave) 0%,
    var(--color-neural-accent) 100%
  );
  color: white;
}

/* ДОБАВИЛ: hover только для action-button */
button.action-button:hover {
  opacity: 0.9;
  transform: scale(1.05);
  box-shadow: 0 5px 15px rgba(126, 87, 194, 0.4);
}

/* Стили для кнопки Назад */
button.back-button {
  background: none !important;
  border: 2px solid var(--color-sea-wave) !important;
  color: var(--color-sea-wave) !important;
  cursor: pointer;
  font-size: 16px;
  padding: 10px 20px;
  margin-bottom: 20px;
  border-radius: 8px;
  transition: all 0.3s ease;
  text-decoration: none;
  display: inline-block;
}

button.back-button:hover {
  background: var(--color-sea-wave) !important;
  color: var(--color-deep-ocean) !important;
  text-decoration: none;
}

/* ДОБАВИЛ: hover для обычных кнопок без классов */
button:not(.action-button):not(.back-button):not(.nav-button):not(
    .logout-button
  ):not(.tab-button):hover {
  background: var(--color-neural-accent);
  color: var(--color-text-light);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(126, 87, 194, 0.3);
}

