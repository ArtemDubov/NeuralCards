/* Специфичные стили для TrainingPage */
.back-button {
  background: none;
  border: none;
  color: var(--color-sea-wave);
  cursor: pointer;
  font-size: 16px;
  padding: 8px 16px;
  margin-bottom: 20px;
  border-radius: 6px;
  transition: all 0.3s ease;
}

.back-button:hover {
  background: var(--color-neural-accent);
  text-decoration: none;
}

/* Специфичные состояния для кнопок викторины */
.btn-tp6.correct {
  border-color: #4caf50;
  background: linear-gradient(135deg, #4caf50, #66bb6a);
  color: white;
  transform: scale(1.02);
}

.btn-tp6.wrong {
  border-color: #f44336;
  background: linear-gradient(135deg, #f44336, #ef5350);
  color: white;
  transform: scale(1.02);
}

.btn-tp6:disabled {
  cursor: not-allowed;
  transform: none;
}

/* Адаптивность */
@media (max-width: 768px) {
  .btn-tp6 {
    min-height: 60px;
    font-size: 1rem;
  }
}

